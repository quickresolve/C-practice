//The frst C program

#include <stdio.h>  //standard library info included

main()              //define a function named main that receives no argument, indicated by empty list
{                   //statements of main are enclosed in braces
    printf("hello, world\n"); //main calls a library function printf; \n newline character
}

// To execute this program in the terminal run cc hello.c, which compiles the program and creates the executable file a.out. Run ./a.out in the terminal to print the sentence "hello, world"